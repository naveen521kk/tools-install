This directory contains configuration for each tool that needs to be installed.

Usually, these are `.toml` files and contains the download links and hashes for items so that they can be easily updated.
